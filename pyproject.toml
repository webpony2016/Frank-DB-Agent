[project]
name = "frank-operations-demo"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["fastapi", "sqlalchemy>=2", "uvicorn", "jinja2", "tzdata", "psycopg[binary]>=3.2,<4"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
